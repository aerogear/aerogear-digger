#!/bin/bash -e
cat <<EOF
This is the android-slave-s2i S2I image:
To use it, install S2I: https://github.com/openshift/source-to-image

Sample invocation:

s2i build <dir-where-accepted-adroid-sdk-is-installed> android-slave-s2i aerogear/jenkins-android-slave

You can then run the resulting image via:
docker run <application image>
EOF
